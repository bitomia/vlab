# Virtual lab

Manage a virtual lab for MacOS as host system.

Dependencies:
* QEMU >= 10.1.0
* socket_vmnet >= 1.2.1

Installing dependencies:

```shell
brew install qemu
brew install socket_vmnet
```
